using UnityEngine;
using System.Collections;

public class GameControllerTest : MonoBehaviour
{

  // Use this for initialization
  void Start()
  {
    var s = GameController.Instance;
    int i = 0;
    var fan = GameController.Instance.GameState;
    int t = i;
  }

  // Update is called once per frame
  void Update()
  {

  }
}
